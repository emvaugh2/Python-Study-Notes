# Defining a function called generate time travel message that will accept the following three parameters: year, destination, and cost. 

def generate_time_travel_message(year, destination, cost):
  return print("""
  Greetings, time traveler! The year is {} and you'll be traveling to {}. This will cost {}.""".format(year, destination, cost)) # message will be created eventually. 